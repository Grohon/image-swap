{
  "name": "imageswap",
  "version": "1.0.4",
  "private": true,
  "description": "ImageSwap Chrome Extension - Build tools",
  "scripts": {
    "build": "node build.js",
    "zip": "node build.js --zip",
    "clean": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('image-swap.zip',{force:true});console.log('Cleaned.')\""
  },
  "devDependencies": {
    "lightningcss": "^1.29.1",
    "terser": "^5.37.0"
  }
}
